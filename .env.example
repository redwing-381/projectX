# ProjectX Environment Variables

# API Authentication (required for CLI access)
# Generate a secure random key, e.g.: python -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY=your-secret-api-key

# Google OAuth (Gmail API)
# Download credentials.json from Google Cloud Console
GOOGLE_CREDENTIALS_PATH=credentials.json
GOOGLE_TOKEN_PATH=token.json

# LLM Provider (OpenRouter recommended)
LLM_API_KEY=sk-or-v1-xxxxxxxxxxxxx
LLM_BASE_URL=https://openrouter.ai/api/v1
LLM_MODEL=openai/gpt-4o-mini

# CrewAI Settings (optional - uses LLM settings if not specified)
CREWAI_MODEL=openrouter/openai/gpt-4o-mini
CREWAI_VERBOSE=false

# Twilio SMS
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_PHONE_NUMBER=+1234567890

# Your keypad phone number (receives SMS alerts)
ALERT_PHONE_NUMBER=+1234567890

# Database (PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/projectx

# App Settings
DEBUG=false
